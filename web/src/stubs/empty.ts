// Stub module used to keep unused optional dependencies (e.g. the BlazePose
// runtime's @mediapipe/pose, which isn't valid ESM) out of the bundle. We
// only use the MoveNet runtime from @tensorflow-models/pose-detection.
export const Pose = undefined;
export const VERSION = undefined;
